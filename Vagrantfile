# -*- mode: ruby -*-
# vi: set ft=ruby :


# vagrantfiles are written using the Ruby programming language.
# Don't worry about that too much---you can usually just add a few things
# to your own vagrantfile and then get everything working.
Vagrant.configure("2") do |config|
  # declare a path to the box.  In our case, it's already on our machine.
  config.vm.box = "C://Users/Public/vagrant/precise64.box"
  #config.vm.provision :shell, path: "bootstrap.sh"
  #config.vm.network :forwarded_port, guest: 80, host: 4567
  #config.vm.provision :shell, path: "bootstrap-web.sh"
  
end
